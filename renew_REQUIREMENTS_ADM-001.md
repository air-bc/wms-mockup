# [要件詳細] ADM-001: ユーザー一覧

> 非機能要件・プロジェクト概要は [renew_REQUIREMENTS.md](renew_REQUIREMENTS.md) を参照すること。
> この画面はサイドメニューではなく、**ヘッダー右上のユーザーエリア**からアクセスする。

---

## 機能概要

登録ユーザーの一覧を表示し、有効/無効の切替・削除・新規登録・編集へのナビゲーションを行う画面。**管理者権限を持つユーザーのみアクセス可能**。削除済み（論理削除）ユーザーは一覧に表示しない。

---

## 機能詳細

### 機能1: ユーザー一覧テーブル

- [ ] 登録ユーザー（削除済みを除く）をテーブル形式で表示する（モックアップのためダミーデータをハードコードで表示）
- [ ] テーブルのカラム:
  - `ログインID`
  - `氏名`
  - `権限`（管理者 / オペレーター）
  - `ステータス`（有効 / 無効）
  - `操作`（編集ボタン・有効/無効切替ボタン・削除ボタン）
- [ ] テーブルヘッダーはスクロール時も常に表示する（sticky）

### 機能2: 有効/無効切替

- [ ] `操作` カラムに **有効/無効切替ボタン** を配置する
- [ ] 現在のステータスに応じてボタンラベルを切り替える（有効→「無効にする」、無効→「有効にする」）
- [ ] クリック時は確認ダイアログを表示し、OKで切替実行する
- [ ] ステータスカラムの表示はトグル後の状態に即時反映する

### 機能3: 削除

- [ ] `操作` カラムに **削除ボタン** を配置する
- [ ] クリック時は確認ダイアログを表示し、OKで論理削除を実行する
- [ ] 削除後、対象ユーザーの行を一覧から除去する

> 削除（論理削除）は有効/無効の状態管理とは独立した操作。削除済みユーザーは一覧から非表示となり、ForeignKeyの参照整合性はデータ上保持する。

### 機能4: ユーザー登録

- [ ] 一覧上部に **「ユーザーを追加」ボタン** を配置する
- [ ] クリック時に ADM-002（新規登録モード）へ遷移する

### 機能5: ユーザー編集

- [ ] `操作` カラムに **編集ボタン** を配置する
- [ ] クリック時に ADM-002（編集モード）へ遷移する

---

## 画面設計

> 技術スタック・共通設計は renew_REQUIREMENTS.md を参照すること。

### ファイル

- **HTML**: `renew_templates/adm_001_user_list.html`
- **CSS**: `static/css/style.css`（流用・追記）
- **JS**: `static/js/main.js`（流用・追記）

### アクセス経路

- ヘッダー右上のユーザーエリア（アバター or ユーザー名）クリック → ドロップダウンメニュー → 「ユーザー管理」

### レイアウト構成

```text
┌──────────────────────────────────────── viewport ────┐
│ [ヘッダー 固定]                       [ユーザー名 ▼] │
├────────┬─────────────────────────────────────────────┤
│ サイド  │ ユーザー管理                                │← ページタイトル
│ バー    │                        [ユーザーを追加]     │← 操作行
│        ├─────────────────────────────────────────────┤
│        │  ログインID │ 氏名 │ 権限 │ ステータス │ 操作 │← sticky
│        │  xxx       │ xxx  │ xxx  │ 有効       │ [編集][無効にする][削除] │
│        │  xxx       │ xxx  │ xxx  │ 無効       │ [編集][有効にする][削除] │
│        │  ...                                        │
└────────┴─────────────────────────────────────────────┘
```

ページは通常スクロール（ユーザー数が少ないためページネーション不要）。

---

### レイアウト実現方式

| 要素                      | 設定                       | 備考                          |
| ------------------------- | -------------------------- | ----------------------------- |
| `<body>`                  | クラス追加なし             | `.schedule-page` は適用しない |
| `<main class="app-main">` | 通常フロー                 | `overflow: visible`           |
| `.history-table-wrapper`  | `overflow-x: auto` のみ    | 縦は制限しない                |
| `thead th`                | `position: sticky; top: 0` | 横スクロール時もヘッダー固定  |

---

### 使用コンポーネント（Atlassian Design System）

| コンポーネント    | 用途                              | トークン・備考                                                               |
| ----------------- | --------------------------------- | ---------------------------------------------------------------------------- |
| Page header       | ページ見出し「ユーザー管理」      | `heading-xl` (1.75rem)                                                       |
| Button (Primary)  | 「ユーザーを追加」ボタン          | ページタイトル右端配置                                                       |
| Card (Raised)     | テーブルセクション外枠            | 既存 `.section-card` 流用                                                    |
| Table             | ユーザー一覧                      | ヘッダー `text-subtle`・sticky                                               |
| Lozenge (Success) | ステータス「有効」バッジ          | `color.background.success`・先頭に ✓ を付与（例: `✓ 有効`）[2026/05/15 修正] |
| Lozenge (Neutral) | ステータス「無効」バッジ          | `color.background.neutral`・先頭に ✕ を付与（例: `✕ 無効`）[2026/05/15 修正] |
| Button (Subtle)   | 編集ボタン（操作カラム）          | 小サイズ                                                                     |
| Button (Subtle)   | 有効/無効切替ボタン（操作カラム） | 小サイズ。ラベルはステータスに応じて切替                                     |
| Button (Danger)   | 削除ボタン（操作カラム）          | 小サイズ                                                                     |
| Inline dialog     | 確認ダイアログ（切替・削除）      | ブラウザ標準 `confirm()` を使用                                              |

---

### テーブルカラム定義

| #   | カラム名   | 幅の目安 | 備考                                |
| --- | ---------- | -------- | ----------------------------------- |
| 1   | ログインID | 8rem     |                                     |
| 2   | 氏名       | auto     |                                     |
| 3   | 権限       | 7rem     | 管理者 / オペレーター               |
| 4   | ステータス | 5rem     | Lozenge で表示                      |
| 5   | 操作       | 14rem    | 編集・有効/無効切替・削除ボタン並列 |

---

### ダミーデータ（8件）

> ステータス: `active`（有効）/ `inactive`（無効）。削除済みは表示しない。

| loginId | 氏名      | 権限         | ステータス |
| ------- | --------- | ------------ | ---------- |
| admin01 | 山田 太郎 | 管理者       | active     |
| admin02 | 鈴木 花子 | 管理者       | active     |
| ope001  | 田中 一郎 | オペレーター | active     |
| ope002  | 佐藤 二郎 | オペレーター | active     |
| ope003  | 伊藤 三郎 | オペレーター | active     |
| ope004  | 渡辺 四郎 | オペレーター | inactive   |
| ope005  | 中村 五郎 | オペレーター | inactive   |
| ope006  | 小林 六子 | オペレーター | active     |

---

### CSS クラス

| クラス名                       | 流用/新規 | 用途                                                                    |
| ------------------------------ | --------- | ----------------------------------------------------------------------- |
| `.section-card`                | 流用      | テーブルセクション外枠                                                  |
| `.section-card-header`         | 流用      | セクション見出し行（`flex`・`space-between`）                           |
| `.history-table-wrapper`       | 流用      | テーブル横スクロールコンテナ                                            |
| `.schedule-table`              | 流用      | ユーザー一覧テーブル                                                    |
| `.schedule-table thead th`     | 流用      | sticky ヘッダー                                                         |
| `.user-control-row`            | 新規      | ページタイトルと「ユーザーを追加」ボタンの行（`flex`・`space-between`） |
| `.user-status-badge`           | 新規      | ステータス Lozenge の共通スタイル                                       |
| `.user-status-badge--active`   | 新規      | 有効バッジ（`color.background.success`）                                |
| `.user-status-badge--inactive` | 新規      | 無効バッジ（`color.background.neutral`）                                |

---

### JS 関数

| 関数名                      | 流用/新規 | 概要                                                                             |
| --------------------------- | --------- | -------------------------------------------------------------------------------- |
| `initUserList()`            | 新規      | ダミーデータの初期化・`renderUserList()` 呼び出し                                |
| `renderUserList()`          | 新規      | `users` 配列（削除済み除く）をテーブルに描画する                                 |
| `toggleUserStatus(loginId)` | 新規      | `confirm()` → OK で該当ユーザーのステータスを切替 → `renderUserList()`           |
| `deleteUser(loginId)`       | 新規      | `confirm()` → OK で該当ユーザーを論理削除（`deleted: true`）→ `renderUserList()` |

#### ダミーデータ構造

```text
const users = [
  { loginId: 'admin01', name: '山田 太郎', role: '管理者', status: 'active', deleted: false },
  ...
]
```

#### `toggleUserStatus` のロジック

1. `loginId` で `users` 配列から対象を特定
2. `confirm('ステータスを切り替えます。よろしいですか？')` → キャンセルで中断
3. `status` を `'active'` ↔ `'inactive'` に切替
4. `renderUserList()` で再描画

#### `deleteUser` のロジック

1. `loginId` で `users` 配列から対象を特定
2. `confirm('このユーザーを削除します。よろしいですか？')` → キャンセルで中断
3. `deleted = true` に設定
4. `renderUserList()` で再描画（`deleted: true` の行は出力しない）

---

### スペーシング規則

| 箇所                                   | トークン    | px相当 |
| -------------------------------------- | ----------- | ------ |
| ページタイトルとテーブルセクションの間 | `space.300` | 24px   |
| テーブルヘッダー・セルの上下パディング | `space.100` | 8px    |
| テーブルヘッダー・セルの左右パディング | `space.150` | 12px   |
| 操作ボタン間                           | `space.050` | 4px    |

---

## 変更履歴

| 日時       | 変更者 | 変更内容                                          |
| ---------- | ------ | ------------------------------------------------- |
| 2026/05/15 | -      | ファイル新規作成（要件定義）                      |
| 2026/05/15 | -      | 画面設計を追記                                    |
| 2026/05/15 | -      | ステータスバッジにアイコン追加（✓ 有効 / ✕ 無効） |
