# [要件詳細] SCR-001: 棚ロケ定義

> 非機能要件・プロジェクト概要は [renew_REQUIREMENTS.md](renew_REQUIREMENTS.md) を参照すること。
> 既存実装の参考: [REQUIREMENTS_SCR-005.md](REQUIREMENTS_SCR-005.md) / [templates/shelf_location.html](templates/shelf_location.html)

---

## 機能概要

倉庫管理者が棚ロケーション・ピッキング経路・バース（荷降ろしエリア）を定義・管理する PC 画面。
3つの機能グループをタブで切り替えて管理する。

| タブ名         | 概要                                           |
| -------------- | ---------------------------------------------- |
| 棚ロケ定義     | 棚ロケのマスタ登録・削除                       |
| ピッキング経路 | 棚ロケのピック順をドラッグ＆ドロップで設定する |

---

## 棚ロケ命名規則

棚ロケコードは任意の文字列で登録する。形式制約なし。

---

## 機能詳細

### 機能1: 棚ロケ定義（タブ1）

#### 棚ロケ一覧

- [ ] 登録済み棚ロケをテーブルで一覧表示する
- [ ] 表示カラム: チェックボックス、棚ロケコード、経路順
- [ ] 経路順はピッキング経路（タブ2）の並び順と連動して表示する（1始まりの整数）
- [ ] 各行の左端にチェックボックスを配置し、チェック/アンチェックできる
- [ ] テーブル右上に「QR印刷」ボタンを配置する（モックアップのためクリック動作は任意）
- [ ] 棚ロケコードの昇順でデフォルト表示する

#### 棚ロケ追加

- [ ] 一覧テーブル上部にインラインフォームを配置する
- [ ] 入力項目: 棚ロケコード（任意の文字列、テキスト入力）
- [ ] 追加ボタン押下で一覧に反映する
- [ ] 同一コードの重複登録は不可とする（エラーメッセージを表示）

#### 棚ロケ削除

- [ ] 各行に削除ボタンを配置する
- [ ] 削除前に確認ダイアログを表示する

---

### 機能2: ピッキング経路（タブ2）

- [ ] 棚ロケ一覧を「ピック順」番号付きで表示する
- [ ] 各行をドラッグ＆ドロップで上下に移動でき、並び替えた順がそのままピック順となる
- [ ] ドラッグ中の行はハイライト表示する
- [ ] ピック順は棚ロケデータに保持し、棚入れ作業時の表示順に反映される（モックアップのためデータの永続化は不要）
- [ ] 表示カラム: ピック順（自動採番）、棚ロケコード

---

## ダミーデータ定義

### 棚ロケ（初期データ）

| 棚ロケコード |
| ------------ |
| A-01-01-1    |
| A-01-01-2    |
| A-01-02-1    |
| A-02-01-1    |
| B-01-01-1    |
| B-01-02-1    |
| C-01-01-1    |

---

---

## 設計

### ファイル

| 種別 | パス | 備考 |
| ---- | ---- | ---- |
| HTML | `renew_templates/001_shelf_location.html` | SCR-001 単体ページ |
| CSS | `static/css/style.css`（流用） | タブ・棚ロケ・D&D 専用スタイルを含む |
| JS | `static/js/main.js`（流用） | 棚ロケ定義セクション（`initShelfLocation` 等）をそのまま使用 |

---

### レイアウト構成

```text
+--------------------------------------------------+
| ページ見出し: 棚ロケ定義                          |
+--------------------------------------------------+
| [棚ロケ定義] [ピッキング経路]                     |  ← .page-tabs（2タブ）
+--------------------------------------------------+
|  .section-card                                   |
|  ┌──────────────────────────────────────────┐   |
|  │ [タブパネル: 棚ロケ定義]                  │   |
|  │  追加フォーム行                           │   |
|  │  棚ロケ一覧テーブル                       │   |
|  │ [タブパネル: ピッキング経路]（非表示）     │   |
|  └──────────────────────────────────────────┘   |
+--------------------------------------------------+
```

タブパネルは1つの `.section-card` 内に並べ、JSで表示/非表示を切り替える。

---

### タブ1: 棚ロケ定義

#### レイアウト詳細

```text
+------------------------------------------------------------------+
|  棚ロケコード [________________]                        [追加]   |
+------------------------------------------------------------------+
|                                              [QR印刷]            |
|  □ | 棚ロケコード  | 操作                                        |
|  □ | A-01-01-1    | [削除]                                       |
|  □ | A-01-01-2    | [削除]                                       |
|  ...                                                             |
+------------------------------------------------------------------+
```

#### 使用コンポーネント

| コンポーネント | クラス | 用途 |
| -------------- | ------ | ---- |
| Page header | `.heading-xl` | ページ見出し「棚ロケ定義」 |
| Tabs | `.page-tabs` / `.page-tab` / `.page-tab.active` | タブバー（2タブ） |
| Card (Raised) | `.section-card` | テーブルを包む外枠 |
| Text input | `.form-input.shelf-input-name` | 棚ロケコード入力欄 |
| Button (Primary) | `.btn.btn-primary` | 追加ボタン |
| Button (Default) | `.btn.btn-default` | QR印刷ボタン（テーブル右上） |
| Table | `.data-table.inventory-table` | 棚ロケ一覧テーブル |
| Checkbox | `<input type="checkbox">` | 行選択・全選択 |
| Button (Danger) | `.btn-delete` | 削除ボタン |

#### テーブルヘッダー行

> [2026/05/08 修正要件により変更済み] 「経路順」列を追加。

| 列 | 内容 | 備考 |
| -- | ---- | ---- |
| 1 | `<input type="checkbox">` 全選択。`onclick="toggleAllShelfChecks(this)"` | |
| 2 | 棚ロケコード | |
| 3 | 経路順 | 表示のみ。ピッキング経路タブの並び順と連動（1始まりの整数） |
| 4 | （空）削除ボタン用 | |

QR印刷ボタンは `.section-action-row`（右寄せ）でフォームとテーブルの間に配置。クリック時は `printShelfQR()` を呼ぶ（`alert()` で代替）。

---

### タブ2: ピッキング経路

#### レイアウト詳細

```text
+----------------------------------------------------------------+
|  ピック順 | 棚ロケコード | ≡（ハンドル）                      |
|  1        | A-01-01-1    | ≡                                  |
|  2        | A-01-01-2    | ≡                                  |
|  ...（ドラッグ&ドロップで並び替え）                            |
+----------------------------------------------------------------+
```

#### 使用コンポーネント

| コンポーネント | クラス・備考 |
| -------------- | ------------ |
| Card (Raised) | 既存 `.section-card` 流用 |
| Table | 既存 `.data-table.inventory-table` 流用 |
| Drag handle | `.drag-handle`（`≡`）。`cursor: grab` |

#### ドラッグ&ドロップ仕様

- 各行の右端にドラッグハンドル（`≡`）を表示する
- 行全体を `draggable="true"` に設定する
- ドラッグ中の行: `.dragging`（`opacity: 0.4`）
- ドロップ位置表示: 上半分 → `.drag-over-top`、下半分 → `.drag-over-bottom`（`2px solid var(--ds-background-brand-bold)`）
- ドロップ完了後にピック順を自動で振り直す

---

### CSS クラス（`static/css/style.css` 流用）

#### タブ

| クラス名 | 用途 |
| -------- | ---- |
| `.page-tabs` | タブバー全体のラッパー |
| `.page-tab` | 個別タブボタン |
| `.page-tab.active` | アクティブタブ |
| `.tab-panel` / `.tab-panel.active` | パネル表示切り替え |

#### 棚ロケ追加フォーム

| クラス名 | 用途 |
| -------- | ---- |
| `.shelf-add-form` | フォーム行ラッパー |
| `.shelf-input-name` | 棚ロケコード入力欄（`width: 200px`、`.form-input` と併用） |
| `.section-action-row` | QR印刷ボタン配置行（右寄せ） |

#### ドラッグ&ドロップ

| クラス名 | 用途 |
| -------- | ---- |
| `.pick-route-row` | ドラッグ可能な棚ロケ行 |
| `.pick-route-row.dragging` | ドラッグ中（`opacity: 0.4`） |
| `.pick-route-row.drag-over-top` | ドロップ位置（上） |
| `.pick-route-row.drag-over-bottom` | ドロップ位置（下） |
| `.drag-handle` | ドラッグハンドルセル（`cursor: grab`） |

#### 削除ボタン

| クラス名 | 用途 |
| -------- | ---- |
| `.btn-delete` | 削除ボタン（`color: --ds-text-danger; border: 1px solid --ds-border-danger`） |
| `.btn-delete:hover` | ホバー時（`background: --ds-background-danger`） |

---

### JS 設計（`static/js/main.js` 流用）

既存の `棚ロケ定義 (SCR-005)` セクションをそのまま流用する。バース関連関数（`renderBerthTable` / `addBerth` / `deleteBerth`）は HTMLに対応要素がないため、既存の `null` チェックにより安全にスキップされる。

#### 使用する関数

| 関数名 | 用途 |
| ------ | ---- |
| `initShelfLocation()` | 画面初期化。`shelfTableBody` が存在しない場合は即リターン |
| `switchShelfTab(tabName)` | タブ切り替え。`'shelf'` / `'route'` を引数に使用する |
| `renderShelfTable()` | 棚ロケ一覧テーブルを再描画する |
| `addShelfLocation()` | 入力値を検証し `shelfLocations` に追加後、テーブルを再描画する |
| `deleteShelfLocation(code)` | 確認ダイアログ後に削除し再描画する |
| `toggleAllShelfChecks(masterCb)` | 全チェックボックスを一括操作する |
| `printShelfQR()` | QR印刷ボタンハンドラ（`alert()` で代替） |
| `renderPickRouteTable()` | ピッキング経路テーブルを描画する |
| `initDragAndDrop()` | 各行へ D&D イベントを設定する |

#### 使用するデータ変数

| 変数名 | 内容 |
| ------ | ---- |
| `shelfLocations` | 棚ロケ配列（ダミーデータ定義の初期値で初期化済み） |
| `dragSrcIndex` | D&D 中のドラッグ元インデックス |

#### HTML 側で必要な要素 ID

| id | 用途 |
| -- | ---- |
| `shelfTableBody` | 棚ロケ一覧 `<tbody>`（初期化トリガー） |
| `shelfCodeInput` | 棚ロケコード入力 `<input>` |
| `panelShelf` / `tabShelf` | タブ1パネル・ボタン |
| `panelRoute` / `tabRoute` | タブ2パネル・ボタン |
| `pickRouteTableBody` | ピッキング経路 `<tbody>` |

---

### スペーシング規則

| 箇所 | トークン | px相当 |
| ---- | -------- | ------ |
| タブバー下マージン | `space.300` | 24px |
| フォーム項目間ギャップ | `space.150` | 12px |
| フォームとテーブルの間 | `space.200` | 16px |
| テーブルセル上下パディング | `space.100` | 8px |
| テーブルセル左右パディング | `space.150` | 12px |

---

## 変更履歴

| 日時 | 変更者 | 変更内容 |
| --- | --- | --- |
| 2026/05/08 | - | 新規作成（既存 SCR-005 の要件を renew 体系へ流用、SCR-001 に再割り振り） |
| 2026/05/08 | - | バース定義（機能3）を削除 |
| 2026/05/08 | - | 設計セクションを追加（DESIGN_SCR-005.md より流用・バース関連除去） |
| 2026/05/08 | - | 棚ロケ一覧テーブルに「経路順」列を追加 |
