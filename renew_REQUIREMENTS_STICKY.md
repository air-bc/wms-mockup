# [要件定義] 付箋メモ機能 (STICKY)

> **AI向け注記**: この機能は全画面に横断的に適用される共通機能。画面固有のSCR番号とは独立して管理する。

---

## メタ情報

| 項目       | 内容         |
| ---------- | ------------ |
| 機能名     | 付箋メモ機能 |
| 作成日     | 2026/05/12   |
| 最終更新   | 2026/05/12   |
| ステータス | 要件定義済み |

---

## 1. 機能概要

モックアップ画面上に付箋を自由配置し、ヒアリング内容・フィードバックをその場で記録・保存できる機能。

---

## 2. 機能要件

### 2.1 付箋の追加・削除

- [ ] 画面右下の「＋」ボタンをクリックすると、新しい付箋が画面中央付近に追加される
- [ ] 1画面に何枚でも付箋を追加できる
- [ ] 付箋右上の「×」ボタンをクリックすると付箋が削除される

### 2.1.1 最小化・復元

- [ ] 付箋ヘッダーに「−」ボタンを設け、クリックすると付箋を最小化する
- [ ] 最小化ボタン（「−」）と閉じるボタン（「×」）の間に十分な間隔を設ける
- [ ] 最小化時は付箋をヘッダーバー（最小化ボタン・閉じるボタンがある帯状エリア）の高さまで縮小する
- [ ] 最小化時はヘッダーバーの背景色を非常に薄くする（背景のみ透明化。ボタンの視認性は維持する）
- [ ] 再度「−」ボタンをクリックすると元のサイズに復元する
- [ ] 最小化状態は localStorage に保存し、ページ再読込後も維持する

### 2.2 テキスト入力

- [ ] 付箋内をクリックすると直接テキスト編集ができる
- [ ] テキスト内容のみ（属性・メタ情報なし）

### 2.3 移動・リサイズ

- [ ] 付箋のタイトルバー部分をドラッグして任意の位置に移動できる（D&D）
- [ ] 付箋の右下コーナーをドラッグしてサイズを変更できる

### 2.4 保存・永続化

- [ ] 付箋データは localStorage に自動保存する
- [ ] ページのファイル名をキーとして、ページごとに付箋データを管理する
- [ ] ページを再読込しても付箋が復元される

### 2.5 エクスポート・インポート

- [ ] 全ページの付箋データをまとめてJSONファイルとしてエクスポートできる
- [ ] エクスポートしたJSONファイルをインポートして付箋を復元できる
- [ ] エクスポート/インポートボタンはUI上の固定位置に配置する
- [ ] 固定ボタン群（＋・エクスポート・インポート）はコンパクトなアイコンボタンとし、`2rem × 2rem`（32px 相当）程度の小さいサイズにする
- [ ] ボタンにはツールチップ（`title` 属性）で機能名を示す

---

## 3. データ構造

localStorage のキー: `wms_sticky_notes`

```json
{
  "scr001.html": [
    {
      "id": "xxx",
      "x": 200,
      "y": 300,
      "w": 200,
      "h": 150,
      "text": "ヒアリング内容...",
      "minimized": false
    }
  ],
  "scr002.html": []
}
```

| フィールド | 型      | 説明                            |
| ---------- | ------- | ------------------------------- |
| id         | string  | 付箋の一意ID（UUID等）          |
| x          | number  | 左上のX座標（px）               |
| y          | number  | 左上のY座標（px）               |
| w          | number  | 幅（px）                        |
| h          | number  | 高さ（px）                      |
| text       | string  | テキスト内容                    |
| minimized  | boolean | 最小化状態（`true` = 最小化中） |

---

## 4. 実装仕様

| 項目         | 内容                                                                  |
| ------------ | --------------------------------------------------------------------- |
| 実装ファイル | `static/js/sticky-notes.js`（新規作成）                               |
| 読み込み     | `renew_templates/` 配下の全HTMLファイルに `<script>` タグを追加       |
| スタイル     | `static/css/sticky-notes.css`（新規作成）またはJS内インラインスタイル |
| 付箋の見た目 | 黄色背景、影付き、タイトルバーあり                                    |
| z-index      | 他の要素より前面に表示（z-index: 9000以上）                           |

---

---

## 5. 設計

### 5.1 ファイル構成

| ファイル                      | 種別       | 内容                                             |
| ----------------------------- | ---------- | ------------------------------------------------ |
| `static/js/sticky-notes.js`   | 新規作成   | 付箋機能の全ロジック                             |
| `static/css/sticky-notes.css` | 新規作成   | 付箋のスタイル定義                               |
| `renew_templates/*.html`      | 既存に追記 | 上記2ファイルの `<script>` / `<link>` タグを追加 |

### 5.2 UI レイアウト

```text
┌──────────────────────────────────────── viewport ────┐
│ [ヘッダー]                                            │
├────────┬──────────────────────────────────────────────┤
│ サイド │  ... 画面コンテンツ ...                       │
│ バー   │                                              │
│        │     ┌─────────────────┐  ← 付箋（黄色）     │
│        │     │ × (削除)        │                      │
│        │     ├─────────────────┤                      │
│        │     │ テキスト入力    │                      │
│        │     │                 │               ◢      │
│        │     └─────────────────┘ ← 右下リサイズハンドル│
│        │                                              │
└────────┴──[ ＋ ][ ↓ ][ ↑ ]──────────────────────────────────┘
              追加 出力 読込
```

- 「＋」「エクスポート（↓）」「インポート（↑）」ボタンは画面**左下**に固定表示（`position: fixed`）
- 付箋は `position: fixed` で viewport 上に重ねて表示する（スクロールに追従しない）
- 付箋は常に他要素より前面に表示する（`z-index: 9000`）

### 5.3 付箋コンポーネント構造

```html
<div
  class="sticky-note"
  id="sticky-{id}"
  style="left:{x}px; top:{y}px; width:{w}px; height:{h}px;"
>
  <div class="sticky-note__header">
    <!-- ドラッグハンドル領域（左側の余白部分） -->
    <span class="sticky-note__drag-area"></span>
    <button class="sticky-note__minimize" aria-label="付箋を最小化">−</button>
    <button class="sticky-note__close" aria-label="付箋を削除">×</button>
  </div>
  <textarea class="sticky-note__body" placeholder="メモを入力...">
{text}</textarea
  >
  <div class="sticky-note__resize-handle"></div>
</div>
```

- ヘッダー内のボタン配置は `display: flex; justify-content: flex-end` とし、ボタン間に `gap: var(--ds-space-100, 0.5rem)` を設ける
- 最小化状態では `.sticky-note--minimized` クラスを付与し、以下のスタイルを適用する

```css
/* 付箋全体をヘッダーバーの高さに縮小する */
.sticky-note--minimized {
  height: auto !important; /* インラインスタイルの height を上書き */
  min-height: 0 !important; /* .sticky-note の min-height: 6rem を無効化 */
  background-color: transparent; /* 付箋本体の背景色を透明にする */
  border-color: transparent; /* 枠線も透明にする */
  box-shadow: none; /* 影も消す */
}

.sticky-note--minimized .sticky-note__body,
.sticky-note--minimized .sticky-note__resize-handle {
  display: none;
}

.sticky-note--minimized .sticky-note__header {
  background-color: rgba(
    253,
    224,
    71,
    0.3
  ); /* ヘッダーバーのみ薄い黄色で表示 */
  border-radius: var(--ds-border-radius-100, 0.25rem); /* 四隅すべてを丸める */
}
```

> 付箋本体（`.sticky-note`）の `background`・`border`・`box-shadow` も透明化しないと、ヘッダーの下に本体の背景色が残って透けて見えない。
> `height: auto !important` が必要な理由: 付箋の高さはJSによってインラインスタイルで設定されており、CSSクラスだけでは上書きできないため。

### 5.4 スタイル設計

| 要素                   | トークン / 値                                                                           | 備考                                                   |
| ---------------------- | --------------------------------------------------------------------------------------- | ------------------------------------------------------ |
| 付箋背景色             | `#fef9c3`（黄色系固定）                                                                 | デザイントークン外。付箋の慣例色                       |
| 付箋ボーダー           | `1px solid #fde047`                                                                     | 同上                                                   |
| 影                     | `var(--ds-shadow-overlay)`                                                              | 浮き上がり感の表現                                     |
| ヘッダー背景           | `#fde047`                                                                               | ドラッグ可能領域を視覚的に区別                         |
| ヘッダーボタン間隔     | `gap: var(--ds-space-100, 0.5rem)`                                                      | 最小化ボタンと閉じるボタンの間隔                       |
| 最小化時ヘッダー背景   | `rgba(253, 224, 71, 0.3)`                                                               | 背景色のみ薄くする。ボタンの視認性は維持               |
| 最小化時 height        | `auto !important`                                                                       | インライン height を上書きしてヘッダーバーの高さに縮小 |
| テキストエリア         | `background: transparent; border: none; resize: none`                                   | 付箋内全体を使う                                       |
| リサイズハンドル       | `position: absolute; right: 0; bottom: 0; width: 1rem; height: 1rem; cursor: se-resize` |                                                        |
| 最小サイズ             | `width: 10rem; height: 6rem`                                                            |                                                        |
| デフォルトサイズ       | `width: 12.5rem; height: 9.375rem`（200×150px相当）                                     |                                                        |
| 固定ボタン群（位置）   | `position: fixed; bottom: var(--ds-space-200); left: var(--ds-space-200)`               |                                                        |
| 固定ボタン群（サイズ） | `width: 2rem; height: 2rem; padding: 0; font-size: 1rem; line-height: 1`                | 32px 相当のコンパクトなアイコンボタン                  |
| 固定ボタン群（形状）   | `border-radius: var(--ds-border-radius-circle)`                                         | 円形ボタン                                             |
| 固定ボタン群（間隔）   | `gap: var(--ds-space-050, 0.25rem)`                                                     | ボタン間の余白を最小限に                               |

### 5.5 JavaScript 設計

#### モジュール構成（`sticky-notes.js`）

```text
StickyNoteManager
  ├── init()              DOMContentLoaded で呼び出し。ストレージから復元・UIを初期化
  ├── _getPageKey()       location.pathname の末尾ファイル名をキーとして返す
  ├── _load()             localStorage から現ページの付箋データを取得
  ├── _save()             全付箋の現在状態を localStorage に書き込む
  ├── add()               新規付箋DOMを生成・追加
  ├── remove(id)          付箋DOMを削除・保存
  ├── toggleMinimize(id)  最小化／復元を切り替え。クラス付与・保存を行う
  ├── _makeDraggable(el)  ヘッダーのmousedownでドラッグ開始・移動・終了
  ├── _makeResizable(el)  ハンドルのmousedownでリサイズ開始・変更・終了
  ├── _onTextChange(id)   textareaの inputイベントで保存をデバウンス呼び出し
  ├── exportJSON()        全ページのデータをJSONファイルとしてダウンロード
  └── importJSON(file)    JSONファイルを読み込み localStorage に上書き・再描画
```

#### 保存タイミング

| イベント                | 処理                         |
| ----------------------- | ---------------------------- |
| 付箋追加・削除          | 即時 `_save()`               |
| ドラッグ終了（mouseup） | 即時 `_save()`               |
| リサイズ終了（mouseup） | 即時 `_save()`               |
| テキスト入力（input）   | 300ms デバウンス後 `_save()` |

### 5.6 エクスポート/インポート仕様

- **エクスポート**: `wms_sticky_notes` キー全体をJSONとしてダウンロード。ファイル名は `sticky-notes-{YYYY-MM-DD}.json`
- **インポート**: `<input type="file" accept=".json">` でファイル選択 → 読み込み後 localStorage を上書き → 現ページの付箋を再描画

---

## 6. 変更履歴

| 日時       | 変更者 | 変更内容                                                                                                         |
| ---------- | ------ | ---------------------------------------------------------------------------------------------------------------- |
| 2026/05/12 | -      | ファイル新規作成                                                                                                 |
| 2026/05/12 | -      | 設計セクション（§5）を追記                                                                                       |
| 2026/05/12 | -      | 固定ボタン群の配置を右下から左下に変更（§2.5、§5.2、§5.4）                                                       |
| 2026/05/12 | -      | 最小化ボタン追加（§2.1.1、§3、§5.3、§5.5）。最小化時は本文非表示・ヘッダー半透明。状態をlocalStorageに保存       |
| 2026/05/12 | -      | 最小化要件を修正（§2.1.1、§5.3）：ボタン間隔の確保・opacity を 0.5→0.3 に変更・ヘッダーのみ表示を明記            |
| 2026/05/12 | -      | 最小化設計を更新（§5.3、§5.4）：ボタン間 gap 追加・opacity 0.3 確定・ヘッダー下辺 border-radius を明記           |
| 2026/05/12 | -      | 最小化要件を修正（§2.1.1）：「ヘッダーのみ表示」をヘッダーバーの高さまで縮小する動作に明確化                     |
| 2026/05/12 | -      | 最小化設計を修正（§5.3、§5.4）：`height: auto !important` でインライン height を上書きしてヘッダーバー高さに縮小 |
| 2026/05/12 | -      | 最小化時 opacity を 0.3→0.1 に変更（§2.1.1、§5.3、§5.4）：下のコンテンツが透けて見える程度に                     |
| 2026/05/12 | -      | 最小化時の透明化を opacity→background-color rgba に変更（§2.1.1、§5.3、§5.4）：背景のみ薄くしボタン視認性を維持  |
| 2026/05/12 | -      | 最小化設計を修正（§5.3）：付箋本体の background・border・box-shadow も透明化。ヘッダーバーのみ rgba で表示       |
| 2026/05/14 | -      | 固定ボタン群のサイズを小型化（§2.5、§5.4）：2rem × 2rem の円形アイコンボタン、gap 最小化                         |
