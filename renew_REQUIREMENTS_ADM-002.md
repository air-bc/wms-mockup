# [要件詳細] ADM-002: ユーザー登録・編集

> 非機能要件・プロジェクト概要は [renew_REQUIREMENTS.md](renew_REQUIREMENTS.md) を参照すること。
> この画面は ADM-001（ユーザー一覧）のサブ画面。

---

## 機能概要

ユーザーの新規登録と既存ユーザーの編集を1つの画面で兼用する。**管理者権限を持つユーザーのみアクセス可能**。新規登録モードと編集モードでフォームの一部挙動が異なる。

---

## モード定義

| モード         | 遷移元                          | 説明                               |
| -------------- | ------------------------------- | ---------------------------------- |
| 新規登録モード | ADM-001「ユーザーを追加」ボタン | 全フィールドが空の状態で表示する   |
| 編集モード     | ADM-001「編集」ボタン           | 対象ユーザーの既存値を初期表示する |

---

## 機能詳細

### 機能1: 入力フォーム

- [ ] 以下のフィールドを配置する:

| フィールド | 種別     | 新規登録       | 編集           |
| ---------- | -------- | -------------- | -------------- |
| ログインID | テキスト | 入力可（必須） | 読み取り専用   |
| 氏名       | テキスト | 入力可（必須） | 入力可（必須） |
| 権限       | セレクト | 選択必須       | 変更可         |

- [ ] 権限セレクトの選択肢: `管理者` / `オペレーター`
- [ ] 編集モードでは、ログインIDフィールドを読み取り専用（`readonly`）にする

### 機能2: 保存

- [ ] フォーム下部に **「保存」ボタン** を配置する
- [ ] 必須フィールドが未入力の場合はバリデーションエラーを表示し、保存を阻止する
- [ ] 保存成功後は ADM-001 へ戻る

### 機能3: キャンセル

- [ ] フォーム下部に **「キャンセル」ボタン** を配置する
- [ ] クリック時は ADM-001 へ戻る（変更を保存しない）

---

## 画面設計

> 技術スタック・共通設計は renew_REQUIREMENTS.md を参照すること。

### ファイル

- **HTML**: `renew_templates/adm_002_user_form.html`
- **CSS**: `static/css/style.css`（流用・追記）
- **JS**: `static/js/main.js`（流用・追記）

### レイアウト構成

```text
┌──────────────────────────────────────── viewport ────┐
│ [ヘッダー 固定]                       [ユーザー名 ▼] │
├────────┬─────────────────────────────────────────────┤
│ サイド  │ ユーザー登録 / ユーザー編集                 │← ページタイトル（モードで切替）
│ バー    │                                             │
│        │ ┌─ ユーザー情報 ────────────────────────┐   │
│        │ │ ログインID  [____________]             │   │
│        │ │ 氏名        [____________]             │   │
│        │ │ 権限        [管理者 ▼   ]             │   │
│        │ │                  [キャンセル] [保存]   │   │
│        │ └───────────────────────────────────────┘   │
└────────┴─────────────────────────────────────────────┘
```

フォームは Card (Raised) 内に配置する。ページは通常スクロール（フィールド数が少ないためスクロール不要）。

---

### モード判定方式

URLクエリパラメータでモードと対象ユーザーを受け取る（静的HTMLのためJSで処理）。

| パラメータ | 新規登録モード | 編集モード       |
| ---------- | -------------- | ---------------- |
| `mode`     | なし（省略）   | `edit`           |
| `id`       | なし（省略）   | 対象の `loginId` |

例: `adm_002_user_form.html?mode=edit&id=ope001`

---

### 使用コンポーネント（Atlassian Design System）

| コンポーネント   | 用途                           | トークン・備考                        |
| ---------------- | ------------------------------ | ------------------------------------- |
| Page header      | ページ見出し（モードで切替）   | `heading-xl` (1.75rem)                |
| Card (Raised)    | フォームセクション外枠         | 既存 `.section-card` 流用             |
| Text field       | ログインID 入力欄              | 編集モード時は `readonly` 属性を付与  |
| Text field       | 氏名 入力欄                    |                                       |
| Select           | 権限選択                       | 選択肢: 管理者 / オペレーター         |
| Inline error     | バリデーションエラーメッセージ | フィールド下部に赤テキストで表示      |
| Button (Subtle)  | 「キャンセル」ボタン           | ADM-001 へ戻る                        |
| Button (Primary) | 「保存」ボタン                 | バリデーション通過後に ADM-001 へ遷移 |

---

### フォームフィールド定義

| フィールド | 要素       | `id`       | 新規登録     | 編集         | バリデーション   |
| ---------- | ---------- | ---------- | ------------ | ------------ | ---------------- |
| ログインID | `<input>`  | `loginId`  | 入力可・必須 | `readonly`   | 必須・空欄不可   |
| 氏名       | `<input>`  | `userName` | 入力可・必須 | 入力可・必須 | 必須・空欄不可   |
| 権限       | `<select>` | `userRole` | 選択必須     | 変更可       | 必須・未選択不可 |

---

### CSS クラス

| クラス名             | 流用/新規 | 用途                                                  |
| -------------------- | --------- | ----------------------------------------------------- |
| `.section-card`      | 流用      | フォームセクション外枠                                |
| `.user-form`         | 新規      | フォーム全体のラッパー                                |
| `.user-form-field`   | 新規      | 各フィールドの行（ラベル + 入力要素 + エラー）        |
| `.user-form-label`   | 新規      | フィールドラベル                                      |
| `.user-form-error`   | 新規      | バリデーションエラーメッセージ（`color.text.danger`） |
| `.user-form-actions` | 新規      | ボタン行（`flex`・`justify-content: flex-end`）       |

---

### JS 関数

| 関数名           | 流用/新規 | 概要                                                                                            |
| ---------------- | --------- | ----------------------------------------------------------------------------------------------- |
| `initUserForm()` | 新規      | URLパラメータを読み取り、編集モードの場合は対象ユーザーの値をフォームに初期セットする           |
| `validateForm()` | 新規      | 必須フィールドの未入力チェック。エラーがある場合は `.user-form-error` を表示して `false` を返す |
| `handleSave()`   | 新規      | `validateForm()` → 通過時に ADM-001（`adm_001_user_list.html`）へ遷移                           |
| `handleCancel()` | 新規      | ADM-001（`adm_001_user_list.html`）へ遷移                                                       |

#### `initUserForm` のロジック

1. `new URLSearchParams(location.search)` でパラメータ取得
2. `mode === 'edit'` の場合:
   - `id` で ADM-001 と共通のダミーデータ配列から対象ユーザーを特定
   - フォームに `loginId`・`userName`・`userRole` を初期セット
   - `#loginId` に `readonly` 属性を付与
   - ページタイトルを「ユーザー編集」に変更
3. `mode` 省略（新規）の場合:
   - フォームは空のまま
   - ページタイトルを「ユーザー登録」とする

#### `validateForm` のロジック

1. `#loginId`・`#userName` が空文字の場合、対応する `.user-form-error` にメッセージを表示
2. `#userRole` が未選択（値が空）の場合、同様にエラー表示
3. エラーがあれば `false`、なければ全エラーをクリアして `true` を返す

---

### スペーシング規則

| 箇所                           | トークン    | px相当 |
| ------------------------------ | ----------- | ------ |
| ページタイトルとカードの間     | `space.300` | 24px   |
| フィールド間                   | `space.200` | 16px   |
| ラベルと入力要素の間           | `space.050` | 4px    |
| 入力要素とエラーメッセージの間 | `space.050` | 4px    |
| カード内パディング             | `space.300` | 24px   |
| ボタン間                       | `space.100` | 8px    |

---

## 変更履歴

| 日時       | 変更者 | 変更内容                     |
| ---------- | ------ | ---------------------------- |
| 2026/05/15 | -      | ファイル新規作成（要件定義） |
| 2026/05/15 | -      | 画面設計を追記               |
