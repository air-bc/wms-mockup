# CLAUDE.md — wms-mockup

> **最重要原則: トークン消費を最小限に抑えること。**
> すべての行動・判断において、この原則を最優先にすること。
> 不要なファイル読み込み・広範な検索・冗長な出力は行わない。
> 「必要な情報だけを、必要な分だけ」取得・生成することを常に意識すること。

## 1. プロジェクト概要

倉庫管理システム（WMS）のUIモックアップを作成するプロジェクト。

## 2. 技術スタック

| 対象         | 使用技術                |
| ------------ | ----------------------- |
| 実装         | HTML / CSS / JavaScript |
| ドキュメント | Markdown                |
| スクリプト   | Python（uv 環境）       |

## 3. ツール利用方針

- ターミナルコマンドは **PowerShell 7 (pwsh)** を使用すること。Bash 構文（`/dev/null`、`$VAR` 等）は使わず、PowerShell 構文（`$null`、`$env:VAR` 等）を厳守する
- Python コマンドを実行する際は **uv 環境**であることを考慮すること（`uv run`・`uv add` 等を使用し、`python` 直接呼び出しは避ける）
- HTML の DOM要素（ID・クラス指定）の参照・編集には **Serena MCP** を活用する（`get_symbols_overview` / `find_symbol` / `replace_symbol_body`）
- Markdown は見出しレベル1のみ認識のため、Serena MCP ではなく Grep + Read を優先する
- スキャン除外ディレクトリ（広範な検索・インデックスの対象外）:
  - `node_modules/`
  - `.git/`
  - `.venv/`

## 4. 実装フロー

すべての実装・修正は以下の手順で進める。

- 各手順を実行する前に**ユーザーに確認を取り、承認を得てから**次の手順に進む
- 各手順で情報が不足している場合は、独断で進めず**ユーザーに質問する**

1. **要件定義** — 要件定義ドキュメントを更新する
2. **設計** — 設計ドキュメントを更新する
3. **実装** — コードを実装する

## 5. Markdown 記述ルール

- コードブロックには**必ず言語を指定**する
- プログラミング言語に該当しない場合（ASCII図、例示、擬似コード等）は `text` を指定する
- 言語指定なしの ` ``` ` は使用禁止

````text
# 良い例
\```text
┌── 構造図 ──┐
\```

# 悪い例
\```
┌── 構造図 ──┐
\```
````

## 6. ドキュメント編集ルール

- 既存内容を単純に上書きせず、**日付とともに修正内容を追記**する
- AIと人間が「既存部分」と「修正部分」を明確に区別できるよう記述する
- 日付フォーマット: `YYYY/mm/dd`

## 6. 対話・出力ルール

- 対話・ソース内コメント・ドキュメント・エラー解説はすべて**日本語**で行う
- 結論から述べる。指定がない限り回答は短くまとめ、冗長な説明を避ける
- 「鋭い指摘です」等の相槌・感想・枕詞は一切不要
- 長文エラーはそのまま出力せず、重要な箇所のみを抽出し、原因と対処法を簡潔に提示する
